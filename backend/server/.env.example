# Database Configuration
# Optimized for high concurrency with connection pooling to prevent P2028 transaction timeout errors
DATABASE_URL="mysql://username:password@localhost:3306/database_name?connection_limit=100&pool_timeout=20&socket_timeout=60"

# Application Configuration
PORT=3001

# JWT Configuration
JWT_SECRET=your-jwt-secret-key-here

# Instructions:
# 1. Copy this file to .env
# 2. Replace the DATABASE_URL with your actual MySQL connection details:
#    - username: your MySQL username
#    - password: your MySQL password  
#    - localhost:3306: your MySQL host and port
#    - database_name: your actual database name
# 3. The connection pool parameters are optimized for high concurrency:
#    - connection_limit=100: Maximum number of connections in the pool
#    - pool_timeout=20: Seconds to wait for a connection from the pool
#    - socket_timeout=60: Seconds before timing out socket operations
