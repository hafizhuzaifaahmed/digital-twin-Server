{
  "name": "digital-twin-server",
  "version": "1.0.0",
  "description": "Digital Twin Server Application",
  "main": "backend/server/dist/main.js",
  "scripts": {
    "postinstall": "cd backend/server && npm ci && npx prisma generate",
    "build": "cd backend/server && npm run build",
    "start": "cd backend/server && npm run start:prod",
    "start:dev": "cd backend/server && npm run start:dev"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "private": true
}